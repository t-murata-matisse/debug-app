import { Typography } from "@mui/material";

const Header = () => {
  return (
    <Typography
      variant="h4"
      align="left"
      className="py-4 font-bold bg-custom1 pl-14"
    >
      DEBUG APP
    </Typography>
  );
};

export default Header;
